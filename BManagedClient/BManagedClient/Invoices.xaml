<Page x:Class="BManagedClient.Invoices"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Invoices" Background="{StaticResource Bg}">
    <Grid Margin="32,28">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <Border Style="{StaticResource EyebrowPill}">
                    <TextBlock Text="BILLING" Style="{StaticResource Eyebrow}"/>
                </Border>
                <TextBlock Text="Invoices." Style="{StaticResource DisplayLg}" Margin="0,10,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="← Back" Click="Back_Click" Style="{StaticResource SecondaryButton}"
                    VerticalAlignment="Top"/>
        </Grid>

        <!-- Left: invoice list -->
        <Border Grid.Row="1" Grid.Column="0" Style="{StaticResource ShellCard}" Margin="0,0,12,0">
            <Border Style="{StaticResource CoreCard}" Padding="6">
                <ListView x:Name="invoiceList" Background="Transparent" BorderThickness="0" SelectionMode="Single"
                          SelectionChanged="Inv_Selected">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="#"      DisplayMemberBinding="{Binding InvoiceNumber}" Width="130"/>
                            <GridViewColumn Header="Status" DisplayMemberBinding="{Binding Status}"        Width="90"/>
                            <GridViewColumn Header="Total"  DisplayMemberBinding="{Binding Total, StringFormat=N0}" Width="90"/>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Border>
        </Border>

        <!-- Right: details -->
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Create new -->
            <Border Grid.Row="0" Style="{StaticResource ShellCard}" Margin="0,0,0,12">
                <Border Style="{StaticResource CoreCard}" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="CUSTOMER (BILL TO)" Style="{StaticResource InputLabel}" Margin="0,0,8,4"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="CURRENCY"          Style="{StaticResource InputLabel}" Margin="0,0,8,4"/>
                        <ComboBox Grid.Row="1" x:Name="newCustomer" Grid.Column="0" Style="{StaticResource ModernCombo}"
                                  DisplayMemberPath="BusinessName" SelectedValuePath="Id" Margin="0,0,8,0"
                                  ToolTip="Pick the customer this invoice is for (their business will appear on the PDF)."/>
                        <ComboBox Grid.Row="1" x:Name="newCurrency" Grid.Column="1" Style="{StaticResource ModernCombo}" Margin="0,0,8,0"
                                  ToolTip="Currency this invoice is billed in. Used for VAT and totals.">
                            <ComboBoxItem Content="ILS" IsSelected="True"/>
                            <ComboBoxItem Content="USD"/>
                        </ComboBox>
                        <Button Grid.Row="1" Grid.Column="2" Content="➕ New invoice" Click="Create_Click" Style="{StaticResource MintButton}"
                                ToolTip="Creates a Draft invoice. Add line items below before sending."/>
                    </Grid>
                </Border>
            </Border>

            <!-- Lines -->
            <Border Grid.Row="1" Style="{StaticResource ShellCard}" Margin="0,0,0,12">
                <Border Style="{StaticResource CoreCard}" Padding="6">
                    <ListView x:Name="lineList" Background="Transparent" BorderThickness="0">
                        <ListView.View>
                            <GridView>
                                <GridViewColumn Header="Description" DisplayMemberBinding="{Binding Description}"  Width="320"/>
                                <GridViewColumn Header="Qty"   DisplayMemberBinding="{Binding Quantity}"            Width="60"/>
                                <GridViewColumn Header="Unit"  DisplayMemberBinding="{Binding UnitPrice, StringFormat=N2}" Width="80"/>
                                <GridViewColumn Header="Total" DisplayMemberBinding="{Binding LineTotal, StringFormat=N2}" Width="100"/>
                            </GridView>
                        </ListView.View>
                    </ListView>
                </Border>
            </Border>

            <!-- Add line -->
            <Border Grid.Row="2" Style="{StaticResource ShellCard}">
                <Border Style="{StaticResource CoreCard}" Padding="14">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <TextBlock Grid.Row="0" Grid.Column="0" Text="DESCRIPTION (WHAT WAS DONE)" Style="{StaticResource InputLabel}" Margin="0,0,6,4"/>
                        <TextBlock Grid.Row="0" Grid.Column="1" Text="QUANTITY (HOURS / UNITS)"   Style="{StaticResource InputLabel}" Margin="0,0,6,4"/>
                        <TextBlock Grid.Row="0" Grid.Column="2" Text="UNIT PRICE (PER UNIT)"      Style="{StaticResource InputLabel}" Margin="0,0,6,4"/>
                        <TextBox  Grid.Row="1" Grid.Column="0" x:Name="lineDesc" Style="{StaticResource ModernTextBox}" Margin="0,0,6,0"
                                  ToolTip="Free text — e.g. 'Logo redesign — round 2'."/>
                        <TextBox  Grid.Row="1" Grid.Column="1" x:Name="lineQty"  Style="{StaticResource ModernTextBox}" Margin="0,0,6,0" Text="1"
                                  ToolTip="How many hours / pieces / sessions. Decimals allowed."/>
                        <TextBox  Grid.Row="1" Grid.Column="2" x:Name="lineUnit" Style="{StaticResource ModernTextBox}" Margin="0,0,6,0" Text="0"
                                  ToolTip="Price per single unit. Line total = Quantity × Unit price."/>
                        <Button   Grid.Row="1" Grid.Column="3" Content="+ Line" Click="AddLine_Click" Style="{StaticResource PrimaryButton}"
                                  ToolTip="Add this row to the current invoice."/>
                        <TextBlock Grid.Row="2" Grid.ColumnSpan="4" Style="{StaticResource Caption}" Margin="0,8,0,0"
                                   Text="Line total = Quantity × Unit price (auto). VAT 18% added on the invoice totals row (0% for Osek Patur)."/>
                    </Grid>
                </Border>
            </Border>

            <!-- Totals + actions -->
            <Grid Grid.Row="3" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="totalsText" Style="{StaticResource Display}" FontSize="22"
                           VerticalAlignment="Center"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal">
                    <Button Content="Mark Sent" Click="Sent_Click" Style="{StaticResource AmberButton}" Margin="0,0,6,0"/>
                    <Button Content="Mark Paid" Click="Paid_Click" Style="{StaticResource MintButton}"  Margin="0,0,6,0"/>
                    <Button Content="📄  Save PDF" Click="Pdf_Click" Style="{StaticResource PrimaryButton}"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
